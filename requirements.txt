# HF Spaces runtime dependencies — minimal set for the Streamlit app.
# Dagster, dbt, Playwright, and dev tools are excluded (not needed at runtime).
# NOTE: streamlit is NOT listed here — HF Spaces pins and installs it automatically.
# numpy pinned <2 because HF Spaces installs streamlit==1.32.0 which requires numpy<2.
duckdb>=0.9.0
pandas>=2.0.0
numpy>=1.24.0,<2
matplotlib>=3.7.0
mplsoccer>=1.6.1
sentence-transformers>=2.5.1
torch>=2.2.0
anthropic>=0.25.0
openai>=1.3.0
google-generativeai>=0.3.0
huggingface-hub>=0.20.0
opik>=1.10.18
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
pyarrow>=14.0.0
requests>=2.31.0
